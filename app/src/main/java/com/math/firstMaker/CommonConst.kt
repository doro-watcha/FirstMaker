package com.math.firstMaker

object CommonConst {

    enum class CollectionType { HOMEWORK, EXAM, PAPER, BOOK, WRONG}
    enum class CollectionStatus { 준비됨, 진행중 , 완료됨 }
    enum class NoteStatus { WRONG, RIGHT}

    enum class UserType { student, teacher }

}

